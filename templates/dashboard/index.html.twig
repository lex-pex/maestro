{% extends 'layouts/app.html.twig' %}
{% block main %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-6">
            <div class="card">
                <div class="card-header">Dashboard</div>
                <div class="cord-body p-2">
                    <a href="{{ path('items') }}">Items Index</a>
                </div>
                <div class="card-footer">acme corp</div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
